import sys
input_data = sys.stdin.read()
lines = input_data.splitlines()
T = int(lines[0])
for i in range(1, T + 1):
    X, N = map(int, lines[i].split())
    points_per_test_case = X // 10
    score = points_per_test_case * N
    print(score)
